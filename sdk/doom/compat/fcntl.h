/*
 * fcntl.h — riscv-stack compat SHADOW header for the DOOM port.
 *
 * i_video.c/i_input.c include <fcntl.h> but use nothing from it; empty
 * stand-in for picolibc-minimal portability (see unistd.h).
 *
 * GPL-2.0-or-later (port glue; see ../ATTRIBUTION.md).
 */
#ifndef RVSTACK_FCNTL_SHADOW_H
#define RVSTACK_FCNTL_SHADOW_H
#endif
