/*
 * SDL2/SDL_ttf.h — shadow header for the sdl2-tetris riscv-stack port.
 * The TTF_* subset lives in sdl2_lite too (built-in 8x8 bitmap font; the
 * .ttf path passed to TTF_OpenFont is ignored — no pak needed).
 * MIT (port glue; inherits the game's license, see ../../LICENSE).
 */
#ifndef RVSTACK_TETRIS_SDL2_TTF_SHADOW_H
#define RVSTACK_TETRIS_SDL2_TTF_SHADOW_H

#include "sdl2_lite.h"

#endif
