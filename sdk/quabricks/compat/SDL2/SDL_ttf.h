/*
 * SDL2/SDL_ttf.h — shadow header for the quabricks riscv-stack port.
 * The TTF_* subset lives in sdl2_lite (built-in 8x8 bitmap font, integer-
 * scaled by ptsize; the .ttf path passed to TTF_OpenFont is ignored — no
 * pak needed).
 * MIT (port glue; inherits the game's license, see ../../LICENSE).
 */
#ifndef RVSTACK_QUABRICKS_SDL2_TTF_SHADOW_H
#define RVSTACK_QUABRICKS_SDL2_TTF_SHADOW_H

#include "sdl2_lite.h"

#endif
